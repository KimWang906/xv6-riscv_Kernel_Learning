# 생각 정리용 파일

scanf()를 통해 배열로 문자를 입력받습니다.

n은 str[]의 길이를 가지고 있습니다.

n = 4;

```code
if(k == n) return 0;
return f(k + 1) + (str[k] - '0');

main() (마지막) -->
호출되었던 지점으로 이동되어 값을 출력합니다.

f(0) -->
if ... skip
// '5' - '0' = 5, 아스키 코드의 값으로 계산하여 정수로 계산되고 f(1)에서 반환된 값 9와 5를 더합니다.
// 함수가 호출되었던 지점(main()으로 이동합니다.)
return f(0 + 1) + (str[0] - '0');

f(1) -->
if ... skip
// '4' - '0' = 4, 아스키 코드의 값으로 계산하여 정수로 계산되고 f(2)에서 반환된 값 5와 4를 더합니다.
// 함수가 호출되었던 지점(f(0))으로 이동합니다.
return f(1 + 1) + (str[1] - '0');

f(2) -->
if ... skip
// '3' - '0' = 3, 아스키 코드의 값으로 계산하여 정수로 계산되고 f(3)에서 반환된 값 2와 3을 더합니다.
// 함수가 호출되었던 지점(f(1))으로 이동합니다.
return f(2 + 1) + (str[2] - '0');

f(3) -->
if ... skip
// '2' - '0' = 2, 아스키 코드의 값으로 계산하여 정수로 계산되고 f(4)에서 반환된 값 0와 2을 더합니다.
// 함수가 호출되었던 지점(f(2))으로 이동합니다.
return f(3 + 1) + (str[3] - '0'); 

f(4) -->
if ... True return 0; // 0을 반환하고 함수가 호출되었던 지점(f(3))으로 이동합니다.
return f(4 + 1) + (str[4] - '0'); // 실행되지 않습니다.
```
